{
  "name": "times4",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "dev": "wrangler dev --persist-to ../.times4-wrangler-state",
    "check": "npm test && wrangler deploy --dry-run",
    "deploy": "npm test && wrangler deploy",
    "golden": "node scripts/regenerate-golden.js"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
